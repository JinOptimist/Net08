@model GameViewModel

<form action="/Game/AddGame" method="post">
    <div>
        NameGame: <input name="Namegame" />
    </div>
    <div>
        Url: <input name="Url" />
    </div>
    <div>
        Link: <input name="Link" />
    </div>
    <div class=" cover-select-genre">
        @for (int i = 0; i < Model.Genres.Count(); i++)
        {
        <div class="name-genre">
            @Html.HiddenFor(m => Model.Genres[i].Id)
            @Html.DisplayFor(x => Model.Genres[i].GenreName)
        </div>
            <div class=" select-genre">
                @Html.CheckBoxFor(x => Model.Genres[i].IsSelected)
            </div>
        }
    </div>
    <div>
        <input type="submit" value="Send" />
    </div>
</form>

